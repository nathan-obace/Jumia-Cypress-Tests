name: My Workflow

on:
  push:
    branches: [main]
  schedule:
    - cron: "10 15 * * *"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Cypress run
        uses: cypress-io/github-action@v5.5.1
